- unless @posts.empty?
  - @posts.each do |post|
    %article.message
      .message-header
        %b= post.employer&.name
        %i= time_ago_in_words(post.published_at) + ' ago'
      .message-body
        %p= post.raw_text
